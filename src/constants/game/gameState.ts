import type { GameStage } from "@/types/menuTypes";

export const STAGE = {
    MAIN_MENU: "main_menu" as GameStage,
    SHOP_ACTIVE: "shop_active" as GameStage,
    EXPLORING: "exploring" as GameStage,
    INN: "inn" as GameStage,
};
